[
  {
    "predicate": "All",
    "rules": [
      { "field": "From", "predicate": "Contains", "value": "noreply@" },
      {
        "field": "Subject",
        "predicate": "DoesNotContain",
        "value": "Promotion"
      }
    ],
    "actions": [{ "type": "mark_as_read" }]
  },
  {
    "predicate": "Any",
    "rules": [
      { "field": "Subject", "predicate": "Contains", "value": "URGENT" },
      { "field": "Message", "predicate": "Contains", "value": "ASAP" }
    ],
    "actions": [{ "type": "move_message", "label": "Important" }]
  },
  {
    "predicate": "All",
    "rules": [
      { "field": "Received", "predicate": "GreaterThanDays", "value": "2" },
      { "field": "from", "predicate": "Contains", "value": "@medium" }
    ],
    "actions": [
      {
        "type": "mark_as_read"
      }
    ]
  }
]
